// // __proto__ & prototype is totally different to each other the __proto__ is a reference(link) to the prtotype to create specific objects and the prototype is created by defult with the functions or objects.

// example :=
// function multiplyBY2(val) {
//   return val * 2;
// }
// multiplyBY2.stored = 5;
// console.log(multiplyBY2(3));  // 6
// console.log(multiplyBY2.stored);  // 5
// console.log(multiplyBY2.prototype);  // {}

// new keyword, new keyword create objects and when we invoke it in the function the function will invoke in the new empty object and whatever object are made he will return that object as well as.

// this keyword, this keyword referse to the global this(window object) let's say i made an object and used the this keyword in that case he will refer to the global object and if i put them into a method then he will refer to his own object.

// // new : new keyword 3 kaam karta hey .
// 1 = object creation.
// 2 = __proto__    // yani object ko hidden method and propeties se connect karna.
// 3 = object ko return karna.
